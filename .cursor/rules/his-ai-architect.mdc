---
description: HIS + 康养平台 AI 架构师工作流 - 需求分析、领域规则、设计规范与自检
alwaysApply: true
---

# 工作流（First Principle）

任何需求进入后，**禁止立即写代码**。须按顺序执行：

1. 需求分析 → 2. 业务分析 → 3. 数据分析 → 4. 权限分析 → 5. AI 分析 → 6. 系统设计 → 7. 开发设计 → 8. 编码实现

若缺少前置文档：停止编码，指出缺失内容。

> 宪法硬约束（AI 只读、双模块、文档驱动等）见 `project-constitution.mdc`，本文档不再重复。

---

# 双模块架构规则

产品层固定为两个可独立部署的模块：

| 模块 | 说明 |
|---|---|
| **HIS** | 医院信息系统：门诊、住院、医嘱、收费等 |
| **康养** | 康养服务系统：院内服务 + 上门服务 |

架构原则：

- 两模块可**独立部署**，共用统一账户体系（SSO）与 API 网关
- 各模块内部优先**模块化单体**，禁止为技术先进而拆分微服务
- 仅当用户量巨大、团队扩大、模块边界稳定后，才允许单模块演进为微服务
- 共享层：账户认证、API 网关、AI 接入层

---

# HIS 领域规则

默认涉及角色：患者、医生、护士、护理员、康养师、服务机构、家属、管理员。

任何设计必须考虑：权限隔离、数据隔离、审计日志。禁止默认开放数据访问。

---

# 医疗数据规则

病历、医嘱、检验/检查结果、护理/康复记录、用户画像等均为敏感数据。须考虑脱敏、权限控制、审计追踪。

---

# AI 原生设计规则

AI 不是外挂。每个业务模块须回答：

1. 人工如何完成
2. AI 如何辅助
3. AI 需要哪些数据
4. AI 调用哪些接口
5. AI 权限边界是什么

未回答以上问题，设计不完整。

---

# 数据设计规则

设计表时须输出：表用途、字段说明、主键、唯一索引、普通索引、外键关系、数据生命周期。

禁止直接输出 CREATE TABLE，先输出设计再输出 DDL。

---

# API 设计规则

所有接口须定义：功能说明、调用方、权限要求、请求/返回参数、错误码、**AI 是否可调用**。

未定义 AI 权限，接口设计不完整。

---

# 前端规则

优先级：微信小程序 → 企业微信小程序 → Web 管理端。

须适配低性能设备、弱网环境，控制首屏加载。禁止炫技式前端设计。

---

# 开发输出规则

输出代码前须声明【依据文档】（README / PRD / Design / Dev 章节）。若无依据，明确标注为推测实现。

---

# 自检（Self Review）

完成任务后执行：业务审查 → 权限审查 → AI 审查 → 安全审查 → 性能审查。

最后输出【风险清单】，而非直接结束。
