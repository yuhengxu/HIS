---
description: HIS + 康养平台 项目核心约束 - 所有开发工作不可偏离的底线
alwaysApply: true
---

# 项目宪法约束

来自 [README.MD](../../README.MD)，所有对话中必须遵守。

## 系统边界

- 系统分 **HIS 医院信息系统** 与 **康养服务系统** 两个模块，可独立部署
- 两模块共用一套账户体系（SSO 单点登录）
- 康养覆盖 **院内服务** 与 **上门服务** 双场景

## 部署与环境

- 支持 Windows 和 Linux 部署
- 支持本地局域网部署和云端部署两种模式
- 前端、后端、数据库须可一键部署（优先 Docker）

## AI 原生与安全（硬约束）

- **数据库只读**：AI/大模型在数据库层仅允许 SELECT 查询，**禁止** INSERT/UPDATE/DELETE/DROP/ALTER/CREATE
- **接口受限**：AI 仅能调用 MCP-API.md 中定义的授权接口子集
- **权限可审计**：所有接口须有文档、权限标注与索引清单

## 前端约束

- 双端覆盖：网页端 + 微信小程序及企业微信小程序
- 轻量优先：适配老旧手机与低配电脑，避免卡顿
- 高并发设计

## 工程约束

- **文档驱动**：需求文档 → 设计文档 → 开发文档 → 代码，禁止跳过文档直接写代码
- **代码溯源**：每次输出代码时，声明所依据的文档章节（如"依据 PRD.md §3.2"）
- **数据可追溯**：所有表/字段/主键/索引须有说明与清单
- **变更联动**：修改代码涉及表/接口/权限变更时，同步更新 design/ 下对应文档
