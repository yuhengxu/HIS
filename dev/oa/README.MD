# OA 基座开发说明

依据：`SAD.md` §3、`Permission-Design.md` §1-§3。

## 首期目标

- 提供 OA 权限域与菜单入口。
- 复用 IAM 的用户、角色、权限点能力。
- 支持按角色分配 OA 权限，并允许对单个用户做权限加授或撤销。

## 开发顺序

1. IAM 权限模型落库。
2. OA 权限点初始化。
3. Web 管理端接入权限菜单。
4. 权限变更审计。
