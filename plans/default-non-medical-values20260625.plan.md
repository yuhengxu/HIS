# 默认值统一非医疗计划（2026-06-25）

## 目标

- 将物资相关表单默认值统一改为非医疗。
- 涉及仓库默认选择、入库新增物资类型默认值、库存新增/调整默认仓库。

## 执行清单

- [x] 发起入库 OA 默认仓库改为非医疗物资，新增物资类型默认非医疗。
- [x] 物品领用默认仓库改为非医疗物资。
- [x] 库存查询中的新增库存、调整库存默认仓库改为非医疗物资。
- [x] 前端构建并重启验证。

## 验证记录

- `frontend/admin npm run build` 通过。
- `scripts/dev-start.sh restart --no-infra --skip-install` 通过，管理端公网地址为 `http://82.156.67.222:15173`。
- 已检查相关前端页面，不再保留默认 `warehouseId: 1` 或默认 `itemType: 'medical'` 初始化值。
