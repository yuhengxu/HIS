# guoxiao 发起入库但 xudong 看不到待办排查计划（2026-06-25）

## 1. 需求分析

- 用户反馈：`guoxiao` 发起入库流程后，`xudong` 登录看不到需要审批的内容。
- 本轮先定位原因，不直接修改业务代码。

## 2. 业务分析

- 入库流程第一节点为“汇报上级审批”。
- 审批待办是否出现在 `xudong`，取决于流程实例创建时解析出的当前处理人是否为 `xudong`。

## 3. 数据分析

- 需要检查 `guoxiao` 的汇报上级字段是否指向 `xudong`。
- 需要检查当前流程任务的 `resolvedSupervisorUserId` 是否为 `xudong` 的用户 ID。

## 4. 权限分析

- `xudong` 需要具备 `oa:task:read` 才能看到待办。
- 如果任务流转到的是其他用户，即使 `xudong` 有审批权限也不会看到该待办。

## 5. AI 分析

- 本轮不涉及 AI 调用。
- 不新增 AI 可访问接口。

## 6. 系统设计排查点

- 检查 `SupervisorResolver` 的上级解析规则。
- 检查入库流程默认节点配置。
- 检查当前用户数据和待办接口返回。

## 7. 执行设计

- 阅读后端流程与用户服务代码。
- 如服务可访问，查询当前用户与待办接口。
- 输出根因和建议修复方向。

## 8. 执行清单

- [ ] 检查流程节点流转规则
- [ ] 检查用户上下级与角色权限
- [ ] 验证当前接口数据
- [ ] 输出原因与处理建议

